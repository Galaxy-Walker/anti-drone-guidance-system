[develop]
# colcon/ament_python 会把生成的 console script 放到 lib/<package> 下，
# 这样 `ros2 run gazebosimulation guidance_node` 才能找到可执行入口。
script_dir=$base/lib/gazebosimulation

[install]
# 安装后的脚本位置与开发模式保持一致。
install_scripts=$base/lib/gazebosimulation
